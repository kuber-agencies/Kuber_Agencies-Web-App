# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment variables (NEVER commit these)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# Build output
dist/
build/

# NOTE: config/db.js is intentionally NOT listed here.
# It must be committed to GitHub so Render can find it.
# Secrets (like MONGO_URI) are set via Render's Environment Variables.
