# Copy this file and rename to .env for local development
# On Render: set these in Dashboard → Environment Variables
# NEVER commit the actual .env file

MONGO_URI=mongodb+srv://your_username:your_password@cluster.mongodb.net/kuber_agencies
PORT=5000
NODE_ENV=development
